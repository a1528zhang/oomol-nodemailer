version: 0.0.1
scripts:
  # Script executed when the container is first loaded
  # eg: bootstrap: pip install pandas
  bootstrap: |
    npm install
    pip install -r requirements.txt
icon: ":twemoji:e-mail:currentColor:"
name: node-mailer
description: Package for NodeMailer
displayName: Node Mailer
releaseNotes: 1. support send mail
